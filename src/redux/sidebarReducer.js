
const initialState = {
  friends: [
    { id: 1, name: 'John' },
    { id: 2, name: 'Bob' },
    { id: 3, name: 'Kate' }
  ]
}
const sidebarReducer = (state = initialState, action) => {

  return state
}

export default sidebarReducer